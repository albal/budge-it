# Webhook-driven builds: point a GitHub push webhook at the EventListener
# route to build on every push to main.
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: budgeit-push
  namespace: budge-it-ci
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    # Triggers CRDs are installed by the wave -1 Subscription mid-sync;
    # see pipeline.yaml for why the dry-run must be skipped.
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  params:
    - name: git-url
      value: $(body.repository.clone_url)
    - name: git-revision
      value: $(body.after)
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: budgeit-build
  namespace: budge-it-ci
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  params:
    - name: git-url
    - name: git-revision
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: budgeit-build-
      spec:
        pipelineRef:
          name: budgeit-build
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-revision)
          - name: version
            value: $(tt.params.git-revision)
        workspaces:
          - name: source
            volumeClaimTemplate:
              spec:
                accessModes:
                  - ReadWriteOnce
                resources:
                  requests:
                    storage: 1Gi
---
apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: budgeit
  namespace: budge-it-ci
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  serviceAccountName: pipeline
  triggers:
    - name: on-push
      bindings:
        - ref: budgeit-push
      template:
        ref: budgeit-build
