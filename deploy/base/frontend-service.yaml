apiVersion: v1
kind: Service
metadata:
  name: budgeit-frontend
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app: budgeit-frontend
spec:
  selector:
    app: budgeit-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
