apiVersion: v1
kind: Service
metadata:
  name: budgeit-backend
  labels:
    app: budgeit-backend
spec:
  selector:
    app: budgeit-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
