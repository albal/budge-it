# Placeholder only — set the real value with an external secret manager or:
#   oc -n budge-it create secret generic budgeit-secrets \
#     --from-literal=OCR_API_KEY=<key> --dry-run=client -o yaml | oc apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: budgeit-secrets
type: Opaque
stringData:
  OCR_API_KEY: "CHANGE_ME"
