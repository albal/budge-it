# pgAdmin does not talk to the Kubernetes API, so no RBAC and no mounted token.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pgadmin
  namespace: pgadmin
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
automountServiceAccountToken: false
