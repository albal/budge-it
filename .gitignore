# --- Go (backend/) ---
backend/budgeit-server
backend/bin/
*.test
*.exe
coverage.out
coverage.html

# --- Node / Vite (frontend/) ---
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
frontend/.vite/
npm-debug.log*
yarn-error.log*

# --- Local development ---
# Environment files (real secrets never belong in git)
.env
.env.*
!.env.example
# Local upload staging dir (LOCAL_STORAGE_DIR fallback)
.uploads/
*.local
# docker-compose volumes if bind-mounted
pgdata/
miniodata/

# --- Editors / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# --- Build / scratch artifacts ---
*.log
tmp/
scratch/
